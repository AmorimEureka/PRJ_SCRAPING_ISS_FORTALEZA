services:
  webserver:
    volumes:
      - ./data:/usr/local/airflow/data

  scheduler:
    volumes:
      - ./data:/usr/local/airflow/data

  triggerer:
    volumes:
      - ./data:/usr/local/airflow/data
